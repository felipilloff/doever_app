targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            app: lib/database/app_database.dart
          schema_dir: drift_schemas
          test_dir: test/migrations
